package Abstract_class;
// abstract classes are those classes which have one or more abstract method.
//these classes needs to be declared as abstract by using abstract classes.
 abstract public class Abstract_example {
    //abstract method have only definition only its body is declared in child classes by overriding mehtod.
    abstract  void partner();
    // abstract method  removes  the drawback of multiple heritance , by using abstract mehtos somehow we are able to
    // to use multiplr inheritance
    abstract  void carreer();
}
